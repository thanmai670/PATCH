"use client";
// TODO(Workstream B): implement. See docs/WORKSTREAM-B.md.
import type { InfectionReport, InfectionNode, TruthChange, Evidence, AgentTraceEntry } from "@/contract";

export function EvidenceRail(props: any) {
  return (
    <div className="p-6 text-sm text-white/40">
      <code>EvidenceRail</code> — not implemented yet.
    </div>
  );
}
